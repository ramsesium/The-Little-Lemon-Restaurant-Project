import React from "react";
import NavFooter from "./NavFooter"; // Assuming NavFooter is in the same directory as Footer
import "../App.css";

const Footer = () => {
  return (
    <footer>
      <NavFooter />
    </footer>
  );
};

export default Footer;

